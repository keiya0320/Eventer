<div class="contents row">
    <div class="more">
      <ul class="more_list">
        <li>
          <%= link_to '編集', edit_event_path(@event.id), method: :get %>
        </li>
        <li>
          <%= link_to '削除', event_path(@event.id), method: :delete %>
        </li>
      </ul>
    </div>
    <%= image_tag @event.image.variant(resize:'300x300') %>
    <span class="name"><%= @event.title %>
    </span>
</div>